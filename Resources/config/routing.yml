con4gisMapsBundle:
    path: /con4gis/api/{_url_fragment}
    defaults:
        _scope: frontend
        _controller: "con4gisMapsBundle:Ajax:ajax"
    requirements:
        module: '\d+'
        action: '.*'

